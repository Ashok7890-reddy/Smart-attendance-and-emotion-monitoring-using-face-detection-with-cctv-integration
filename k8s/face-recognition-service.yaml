apiVersion: apps/v1
kind: Deployment
metadata:
  name: face-recognition-service
  namespace: smart-attendance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: face-recognition-service
  template:
    metadata:
      labels:
        app: face-recognition-service
    spec:
      containers:
      - name: face-recognition-service
        image: smart-attendance/face-recognition:latest
        ports:
        - containerPort: 8000
        env:
        - name: SERVICE_NAME
          value: "face_recognition"
        envFrom:
        - configMapRef:
            name: smart-attendance-config
        - secretRef:
            name: smart-attendance-secrets
        volumeMounts:
        - name: models-storage
          mountPath: /app/models
        - name: logs-storage
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
      volumes:
      - name: models-storage
        persistentVolumeClaim:
          claimName: models-pvc
      - name: logs-storage
        persistentVolumeClaim:
          claimName: logs-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: face-recognition-service
  namespace: smart-attendance
spec:
  selector:
    app: face-recognition-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP