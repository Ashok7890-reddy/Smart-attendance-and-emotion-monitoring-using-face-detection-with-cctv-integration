apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-service
  namespace: smart-attendance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: attendance-service
  template:
    metadata:
      labels:
        app: attendance-service
    spec:
      containers:
      - name: attendance-service
        image: smart-attendance/attendance-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: SERVICE_NAME
          value: "attendance_management"
        envFrom:
        - configMapRef:
            name: smart-attendance-config
        - secretRef:
            name: smart-attendance-secrets
        volumeMounts:
        - name: logs-storage
          mountPath: /app/logs
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      volumes:
      - name: logs-storage
        persistentVolumeClaim:
          claimName: logs-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: smart-attendance
spec:
  selector:
    app: attendance-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP